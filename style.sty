\usepackage{amsmath}
\usepackage{tcolorbox}   
\usepackage{ifthen}
\usepackage{graphicx}    
\usepackage{caption}    

\usepackage{pdfpages} % notwendig zum Einbinden von PDF-Seiten
\usepackage{hyperref} 
\usepackage{pdflscape}

% Kodierung und Sprache
\RequirePackage[utf8]{inputenc}
\RequirePackage[T1]{fontenc}
\RequirePackage[ngerman]{babel}
\RequirePackage{Versuchsinformationen_PAP2_Gruppe-20} 

\RequirePackage{pdfpages}

\RequirePackage{url}

\RequirePackage{xcolor}

%Heidelberg predefinded
\definecolor{SiegelHintergrund}{RGB}{198,24,38}
\definecolor{SiegelOutline}{RGB}{89,13,8}
\definecolor{Sand}{RGB}{244,241,234}

% Colors for PAP1
\definecolor{BluePrime}{RGB}{51, 102, 204}
\definecolor{BlueSec}{RGB}{102, 153, 255}
\definecolor{BlueTri}{RGB}{0, 51, 153}

%Colors for PAP2
\definecolor{GreenPrime}{RGB}{27, 169, 127} % Title
\definecolor{GreenSec}{RGB}{27, 114, 47} % Dark
\definecolor{GreenTri}{RGB}{103, 218, 184} % Light

% Font
\RequirePackage{lmodern}

\RequirePackage[
  a4paper, % Papierformat
  top=1.5cm,
  bottom=1.5cm,
  left= 1.5cm,
  right= 1.5cm,
  includehead, % Kopfzeile in Randberechnung
  includefoot, % Fußzeile in Randberechnung
  headheight=13.75pt % Höhe der Kopfzeile
]{geometry}


% Mathe und Einheiten
\RequirePackage{amsmath,amssymb}
\RequirePackage{siunitx}

% Grafiken
\RequirePackage{graphicx}
\RequirePackage{tikz}

% Überschriften
% \renewcommand\thechapter{\Roman{chapter}}
\renewcommand\thechapter{\arabic{chapter}.}
\renewcommand\thesection{\arabic{chapter}.\arabic{section}}
\addtokomafont{chapter}{\color{GreenPrime}}
\addtokomafont{section}{\color{GreenPrime}}

% Grafiken
\renewcommand{\thefigure}{\Roman{figure}}
\usepackage[
  labelfont=bf,                
  textfont=it,                 
  labelsep=colon,              
  justification=raggedright,   
  singlelinecheck=false        
]{caption}


% Formeln
\renewcommand\theequation{\arabic{equation}}

% KOMA-Paket für Kopf-/Fußzeilen
\RequirePackage[automark,headsepline,footsepline]{scrlayer-scrpage}

\clearpairofpagestyles

% Automatische Kapitelmarken aktivieren
\automark[chapter]{chapter}


% Kopfzeile
\ihead{\raisebox{0.1cm}[0pt][0pt]{Finn Zeumer (hz334)}}
\chead{\raisebox{0.1cm}[0pt][0pt]{\versuchsname{\versuchsnummer}}}
\ohead{\raisebox{0.1cm}[0pt][0pt]{\headmark}}

% Fußzeile
\cfoot{\raisebox{-0.25cm}[0pt][0pt]{Seite \pagemark}}

% Auf allen Seiten anwenden
\pagestyle{scrheadings}

% Kapitelanfang auch mit gleichen Kopfzeilen (keine Plain-Seiten mehr)
\renewcommand*{\chapterpagestyle}{scrheadings}


% Links und Referenzen
\RequirePackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=GreenPrime,
    citecolor=GreenPrime,
    urlcolor=GreenPrime
}

\setlength{\columnsep}{1em}     % Abstand zwischen den Spalten
\setlength{\columnseprule}{0pt} % Stärke der Trennlinie (0pt = keine Linie)

% Tabellen design
\RequirePackage{booktabs}
\RequirePackage{siunitx}
\RequirePackage{longtable}

\RequirePackage[table]{xcolor}


\sisetup{
  detect-all,
  table-number-alignment = center,
  separate-uncertainty = true
}

